<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <link rel="stylesheet" href="/stylesheets/style.css">

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen main bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input type="text" class="block w-full rounded-md outline-none px-3 py-2 border" name="title"
                    placeholder="Title goes here...">
                <textarea class="block w-full mt-3 rounded-md resize-none outline-none px-3 py-2 border" name="details"
                    placeholder="Write your task details here..."></textarea>
                <input class="bg-blue-600 px-7 py-2 mt-3 rounded-md cursor-pointer" value="Create Task" type="submit">
            </form>
        </div>

        <div class="tasks flex items-start gap-4 flex-wrap p-10">
            <% if (files && files.length> 0) { %>
                <% files.forEach((val)=> { %>
                    <div class="task min-w-72 px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-2xl tracking-lighter">
                            <%= val %>
                        </h1>
                        <a class="text-blue-500 inline-block mt-3" href="/file/<%= val%>">Read more..</a>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <h1 class="text-zinc-700 font-bold text-xl">No tasks yet.</h1>
                            <% } %>
        </div>
    </div>

    <script src="/javascripts/script.js"></script>
</body>

</html>